#include <iostream>

/*
    binary search
*/

void binarySearch(int *arr, int size, int target){

    int x = size/2;
    if(target < arr[x]){
        for(int i = 0; i < x; i++ ){
            
        }
    }
}